{
  "private": true,
  "scripts": {
    "build": "parcel build src/manifest.json --no-cache --no-source-maps",
    "test": "jest"
  },
  "devDependencies": {
    "@parcel/config-webextension": "^2.10.3",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "jsdom": "^23.0.1",
    "parcel": "^2.10.3"
  }
}
